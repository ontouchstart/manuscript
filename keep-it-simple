It is hard to keep it simple.

This is a simple text file with no markup.

This file has no extension as a test to see if it can be rendered the way it is.
(It doesn't because browser need content type. We will stick to .txt.)